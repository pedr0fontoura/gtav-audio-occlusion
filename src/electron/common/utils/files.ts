export const isXMLFilePath = (path: string): boolean => path.includes('.xml');

export const isMapTypesFilePath = (path: string): boolean => path.includes('.ytyp');

export const isMapDataFilePath = (path: string): boolean => path.includes('.ymap');
